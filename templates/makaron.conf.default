# Makaron configuration file
# ~/.config/makaron/makaron.conf

# Battery low threshold (percentage)
BATTERY_LOW_THRESHOLD=20

# SketchyBar: hide CPU / memory / storage widgets (more room for workspaces)
SKETCHYBAR_COMPACT_MODE=false

# SketchyBar: hide empty AeroSpace workspaces from the bar
# The focused workspace is always drawn, even if it's empty.
SKETCHYBAR_HIDE_EMPTY_WORKSPACES=true

# AeroSpace trackpad swipe: number of fingers to switch workspaces
AEROSPACE_SWIPE_FINGERS=4

# AeroSpace trackpad swipe direction. true = match macOS (swipe left -> next
# workspace); false = reversed.
AEROSPACE_SWIPE_NATURAL=true

# Desktop theme. Run `makaron-theme list` to see the seven available themes.
MAKARON_THEME=glass-light

# Theme side effects. Ghostty and editor themes remain user-owned.
THEME_SET_WALLPAPER=true
THEME_SET_MACOS_APPEARANCE=true

# Window layout. A gap applies independently of window borders.
BORDERS_ENABLED=false
BORDER_WIDTH=5
AEROSPACE_GAP_SIZE=0
