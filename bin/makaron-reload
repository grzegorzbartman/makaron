#!/bin/bash
MAKARON_PATH="${MAKARON_PATH:-$HOME/.local/share/makaron}"

if command -v aerospace &>/dev/null && pgrep -f "AeroSpace" >/dev/null 2>&1; then
    aerospace reload-config 2>/dev/null || true
fi

pkill -f makaron-bar 2>/dev/null || true
sleep 0.3
"$MAKARON_PATH/bin/makaron-bar" &
disown
